# ---------------------------------------------------------------------------
# Outer 环境变量模板
#
# 使用方式：cp .env.example .env 然后填入真实值。
# 规则（详见 docs/CONVENTIONS.md「密钥规范」）：
#   1. .env 永不提交，本文件只写「键名 + 说明」，绝不写真实值。
#   2. 只有确认可公开的值才允许用 VITE_ 前缀（该前缀会被 Vite 打进前端产物）。
#   3. 服务端密钥禁止使用 VITE_ 前缀。
#   4. 新增任何环境变量，必须同步补充到本文件，并更新 Vercel 环境变量配置。
# ---------------------------------------------------------------------------

# --- 前端可见（公开值，依赖 RLS 保护数据安全）---

# Supabase 项目 URL，形如 https://xxxxxxxx.supabase.co
VITE_SUPABASE_URL=

# Supabase 匿名公钥（anon key）。设计上可公开，但这要求所有表的 RLS 策略必须严格；
# 切勿在此处填入 service_role key。
VITE_SUPABASE_ANON_KEY=

# --- 仅服务端可见（严禁泄露到前端）---

# DashScope（阿里云百炼）API Key，供 api/dashscope.js 在服务端调用 AI 使用。
# ⚠️ 服务端专用：禁止使用 VITE_ 前缀命名（VITE_ 变量会被打包进前端产物，等同于公开密钥）。
# ⚠️ 本变量的历史命名 VITE_DASHSCOPE_API_KEY 已废弃，见 docs/DECISIONS.md ADR-004。
# 配置位置：本机 .env 与 Vercel 项目环境变量（Environment Variables）。
DASHSCOPE_API_KEY=
